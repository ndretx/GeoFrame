import { View, Text } from "react-native";

export default function CameraPage({navigation, route}){
    return(
        <View>
            <Text>
                Camera Page
            </Text>
        </View>
    ) 
}