export default class MarkerEntity{
    
    id: string;
    title: string;
    imagePath: string;
    coords: { latitude: number, longitude: number}
    description: string;
    photoDate: string;

    
}